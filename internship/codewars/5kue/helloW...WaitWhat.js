var helloWorld = function () {
    const Hell = () => { };
    const o = () => { };
    const W = (a, b) => { };
    const orld = (a, b, c) => { };

    const space = orld.length.toString() + W.length.toString();
    const exclamationMark = orld.length.toString() + orld.length.toString();

    return Hell.name + o.name + String.fromCharCode(+space) + W.name + orld.name + String.fromCharCode(+exclamationMark)
}

console.log(helloWorld());